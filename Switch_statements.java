
import java.util.Scanner;

public class Switch_statements {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        // .equals() -> checks the value but '==' checks for the reference and gives false
        /*
         * Syntax for Switch statements:
         * switch(expression){
         *  case 1:
         *      //body
         *      break;
         *  case 2:
         *      //body
         *      break;
         *  deafult:
         *      //body
         * }
            Enhanced switch case:
            switch(expression){
                case 1 -> //body;
                case 2 -> //body;
                case 3 -> //body;
                case 4 -> //body;
                default -> //body;
            }
         */
        in.close();
    }
}
